name: reviewdog
on: [pull_request]
jobs:
    eslint-backend:
        name: eslint / backend
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - working-directory: backend
            - uses: actions/setup-node@v3
            - run: npm install
            - run: npm run lint 
